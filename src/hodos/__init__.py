"""Hodos — an open-source engine for customer-journey intelligence.

This package currently ships one extracted module: `hodos.publish`, a
pluggable-destination abstraction for pushing rendered output somewhere
(a local directory, a GitHub Pages branch, or nowhere at all). More
modules follow as they're extracted from the private application this
engine is being distilled out of. See README.md for the current status.
"""

__version__ = "0.1.0"
